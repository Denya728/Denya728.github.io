(function(){'use strict';function kill(){const r=document.getElementById('content');if(!r)return;const t=(document.getElementById('title')?.textContent||'').toLowerCase();if(!t.includes('inventario')&&!t.includes('compras'))return;r.querySelectorAll('*').forEach(el=>{const x=(el.textContent||'').trim();if(!x||x.length>80)return;const s=getComputedStyle(el),f=parseFloat(s.fontSize)||0,p=s.position,c=s.color,o=parseFloat(s.opacity);const white=c.indexOf('255, 255, 255')>=0||c.toLowerCase()==='#fff'||c.toLowerCase()==='#ffffff';if(/inventario|compras/i.test(x)&&f>=32&&(p==='absolute'||p==='fixed'||o<0.5)&&white)el.style.setProperty('display','none','important')})}kill();new MutationObserver(kill).observe(document.getElementById('content')||document.body,{childList:true,subtree:true});window.addEventListener('load',kill)})();